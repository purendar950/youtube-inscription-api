# YouTube Transcript API - Environment Config
# Copy to .env and fill in if needed

# Host & Port
HOST=0.0.0.0
PORT=8000

# Logging
LOG_LEVEL=INFO

# Optional: Rate limiting (requests per minute per IP)
# RATE_LIMIT=60

# Optional: API Keys for access control
# API_KEY=your-secret-key
